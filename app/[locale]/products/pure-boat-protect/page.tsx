import type{Metadata}from'next';import{notFound}from'next/navigation';import PureBoatProtectPage from'@/app/pure-boat-protect/page';export const metadata:Metadata={title:'PURE Boat Protect | Premium Yacht Surface Protection | PURE'};export default function EnglishBoatProtectPage({params}:{params:{locale:string}}){if(params.locale!=='en')notFound();return <PureBoatProtectPage/>}
