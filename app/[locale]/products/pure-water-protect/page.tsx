import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import PureWaterProtectPage from '@/app/pure-water-protect/page'; export const metadata:Metadata={title:'PURE WATER PROTECT | Marine Easy-to-Clean Technology | PURE'}; export default function EnglishWaterProtectPage({params}:{params:{locale:string}}){if(params.locale!=='en')notFound();return <PureWaterProtectPage/>}
