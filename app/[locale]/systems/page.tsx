import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import Link from 'next/link'; export const metadata:Metadata={title:'System Routes · PURE Technology Platform'}; export default function EnglishSystems({params}:{params:{locale:string}}){if(params.locale!=='en')notFound();return <section className="section"><div className="container"><div className="section-heading section-heading--narrow"><span className="section-heading__eyebrow">SYSTEM ROUTES</span><h1>Defined systems for specific applications.</h1><p>PURE technologies are not considered as isolated material properties. Defined system build-ups, suitable substrates, application conditions and transparent validation pathways are essential.</p></div><div className="system-routes-index">{[['PURE THERMO INTERIOR','/en/systems/pure-thermo-interior'],['PURE THERMO EXTERIOR','/en/systems/pure-thermo-exterior'],['PURE THERMO DETAIL','/en/systems/pure-thermo-detail'],['PURE THERMO + PURE FIRE','/en/systems/pure-thermo-fire']].map(([title,href])=><article key={title}><h2>{title}</h2><p>Defined system and validation route with its existing development status.</p><Link className="btn btn--primary" href={href}>View system</Link></article>)}</div></div></section>}
