import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import PureWaterProtectPage from '@/app/pure-water-protect/page'; export const metadata:Metadata={title:'PURE Water Protect | Marine Easy-to-Clean Surface Technology | PURE'}; export default function GermanWaterProtectPage({params}:{params:{locale:string}}){if(params.locale!=='de')notFound();return <PureWaterProtectPage/>}
