import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import Link from 'next/link'; import { ThermoSystemRoutePage } from '@/components/ThermoSystemRoutePage'; import { TechnicalVisual } from '@/components/TechnicalVisual';
export const metadata:Metadata={title:'Systemrouten · PURE Technology Platform'};
export default function GermanSystems({params}:{params:{locale:string}}){if(params.locale!=='de')notFound();return <section className="section"><div className="container"><div className="section-heading section-heading--narrow"><span className="section-heading__eyebrow">SYSTEMROUTEN</span><h1>Definierte Systeme für konkrete Anwendungen.</h1><p>PURE Technologien werden nicht als isolierte Materialeigenschaften betrachtet. Entscheidend sind definierte Systemaufbauten, geeignete Untergründe, Anwendungsbedingungen und nachvollziehbare Prüfpfade.</p></div><div className="system-routes-index">{[['PURE THERMO INTERIOR','/de/systeme/pure-thermo-interior'],['PURE THERMO EXTERIOR','/de/systeme/pure-thermo-exterior'],['PURE THERMO DETAIL','/de/systeme/pure-thermo-detail'],['PURE THERMO + PURE FIRE','/de/systeme/pure-thermo-fire']].map(([title,href])=><article key={title}><h2>{title}</h2><p>Definierte System- und Prüfroute mit bestehendem Entwicklungsstatus.</p><Link className="btn btn--primary" href={href}>System ansehen</Link></article>)}</div></div></section>}
